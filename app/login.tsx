import React from "react";
import { Text, View } from "react-native";

const LoginScreen = () => {
  return (
    <View>
      <Text style={{ color: "white", fontSize: 48 }}>Login</Text>
    </View>
  );
};

export default LoginScreen;
